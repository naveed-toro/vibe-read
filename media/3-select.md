One reason catches your eye and you want to see what it actually did.

Select those lines and press `Alt+X`. It works on your selection instead of
the file:

```python
# Round once, at the end. Rounding at each step drifts by a few cents.
return round(subtotal * (1 + tax_rate), 2)     ← back, just this one
🙈                                              ← the rest stays away
```

Press it again on the same lines to put them back.

The same key, narrowed to whatever you picked. There is nothing else to learn.
