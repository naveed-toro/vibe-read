Before:

```python
# Discount before tax.
if coupon:
    subtotal *= (1 - rate)
```

After `Alt+X`:

```python
# Discount before tax.
🙈
🙈
```

Nothing was edited. Press `Alt+X` again and every
line comes back exactly as it was.
